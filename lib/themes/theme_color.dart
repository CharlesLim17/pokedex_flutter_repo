import 'package:flutter/widgets.dart';

class ThemeColor {
  const ThemeColor._();

  static const Color primary = Color(0xFF2B74C7);
  // for headers and titles
  static const Color secondary = Color(0xFF191414);

  // for labels of fields and small texts
  static const Color tertiary = Color(0xFF747683);

  // for field color
  static const Color netural = Color(0xFFE1E8ED);

  // background color
  static const Color background = Color(0xFFF5F8FA);

  static const Color error = Color.fromARGB(255, 240, 99, 12);
}
